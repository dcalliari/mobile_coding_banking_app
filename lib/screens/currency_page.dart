import 'package:flutter/material.dart';

class MyCurrencyPage extends StatelessWidget {
  const MyCurrencyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Currency Page'),
      ),
    );
  }
}
